# --- Android Studio 默认生成部分 ---
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
/build
/captures
.externalNativeBuild
.cxx

# --- 必须检查是否包含的部分 ---
# Mac 系统生成的垃圾文件 (非常重要，否则 Mac 用户很容易提交这个)
.DS_Store

# Gradle 构建产物
.gradle/
build/
app/build/

# 本地配置文件 (不应上传，因为包含 SDK 路径)
local.properties

# IDE 配置 (除了 code style 等共享配置，其他不应上传)
.idea/
*.iml